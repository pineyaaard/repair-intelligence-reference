# The deterministic browser demo requires no secrets.
PORT=4173

# Optional server-side GPT prefill. Never expose this value to browser code.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6

# Required only when OPENAI_API_KEY is set. Use a new random value per deployment.
SAFETY_ID_SALT=
